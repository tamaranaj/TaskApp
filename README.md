## How to run the server

npm install
Run PG Admin
Create a new database called tasks
Copy all properties from .env.example to .env and change them to your needs
npm run start (If you want to change something and see changes, just run npm run start:dev)
Api endpoints: localhost:3000/

## How to run the web

npm install
npm start
Open link in browser
